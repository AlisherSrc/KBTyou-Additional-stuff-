#include <iostream>
#include <vector>
#include <map>

using namespace std;

int main(){
    map<string,vector<int> > m;
    int n;
    map<string,vector<int> >:: iterator it;
    for(int i = 0;i<n;i++){
        m["Hello"] = make_pair();
    }

    return 0;
